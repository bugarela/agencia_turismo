<%= form_with(model: fundacao, local: true) do |form| %>
  <% if fundacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fundacao.errors.count, "error") %> prohibited this fundacao from being saved:</h2>

      <ul>
      <% fundacao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :museu_id %>
    <%= form.text_field :museu_id %>
  </div>

  <div class="field">
    <%= form.label :fundador_id %>
    <%= form.text_field :fundador_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
